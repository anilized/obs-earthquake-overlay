/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If deploying under a subpath, set `basePath` here.
  // basePath: '',
  // output: 'standalone', // optional for containerized deploys
};

export default nextConfig;
